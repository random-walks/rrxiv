# LaTeX build artifacts
*.aux
*.log
*.out
*.toc
*.bbl
*.blg
*.synctex.gz
*.fdb_latexmk
*.fls

# But DO commit the .pdf since it's a release artifact (optional - your call)
# *.pdf

# Track sidecar files inside the conformance suite (otherwise *.aux above
# matches *.rrvix.aux and the suite ships without its expected sidecars).
!tests/conformance/fixtures/**/*.rrvix.aux

# Schema validation cache
.schema-cache/

# MkDocs build output
site/
.cache/

# Editor and OS
.DS_Store
.vscode/
.idea/
*.swp
